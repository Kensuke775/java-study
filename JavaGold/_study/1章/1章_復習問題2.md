# 1章 復習問題2(1回目で不正解 or `!`が付いた問題の再挑戦・別バージョン)

`1章_復習問題.md`で不正解だった/`!`が付いた8問と**同じ概念**を、別のクラス名・別の値・別の状況で出題し直したもの(選択肢も5択に増量)。javac/javaで動作検証済み。ラムダ式は使用していません。答えは書かず、問題のみ記載。

## 目次

- [復習2-1b](#復習2-1b)
- [復習2-2b](#復習2-2b)
- [復習3-3b](#復習3-3b)
- [復習3-4b](#復習3-4b)
- [復習5-1b](#復習5-1b)
- [復習5-2b](#復習5-2b)
- [復習6-1b](#復習6-1b)
- [復習6-2b](#復習6-2b)

<br><br>

<a id="復習2-1b"></a>
### 復習2-1b

```java
1  import java.util.ArrayList;
2  import java.util.List;
3
4  public class Main {
5      static class Animal {}
6      static class Dog extends Animal {}
7
8      static void feed(List<? extends Animal> list) {
9          list.add(new Dog());
10     }
11     public static void main(String[] args) {
12         List<Dog> dogs = new ArrayList<>();
13         feed(dogs);
14     }
15 }
```

9行目を実行するとどうなるか。

A. `dogs`に新しい`Dog`が追加される

B. 9行目で`UnsupportedOperationException`がスローされる

C. 9行目でコンパイルエラーになる

D. 9行目で`ClassCastException`がスローされる

E. 8行目でコンパイルエラーになる

<a id="復習2-2b"></a>
### 復習2-2b

```java
1  import java.util.List;
2
3  public class Main {
4      static void printAll(List<?> list) {
5          list.add(Double.valueOf(1.5));
6      }
7      public static void main(String[] args) {
8          printAll(List.of(1, 2, 3));
9      }
10 }
```

5行目を実行するとどうなるか。

A. `list`に`1.5`が追加される

B. 5行目で`UnsupportedOperationException`がスローされる

C. 5行目でコンパイルエラーになる

D. 8行目で`ClassCastException`がスローされる

E. 5行目で`NullPointerException`がスローされる

<a id="復習3-3b"></a>
### 復習3-3b

```java
1  import java.util.HashSet;
2  import java.util.Set;
3
4  public class Main {
5      public static void main(String[] args) {
6          Set<Integer> set = new HashSet<>();
7          set.add(100);
8          boolean removed = set.remove(100L);
9          System.out.println("removed=" + removed + " set=" + set);
10     }
11 }
```

8行目・9行目を実行するとどうなるか。

A. `removed=true set=[]`

B. `removed=false set=[100]`

C. 8行目でコンパイルエラーになる

D. 8行目で`ClassCastException`がスローされる

E. 8行目で`NumberFormatException`がスローされる

<a id="復習3-4b"></a>
### 復習3-4b

```java
1  import java.util.ArrayList;
2  import java.util.List;
3
4  public class Main {
5      public static void main(String[] args) {
6          List<String> list = new ArrayList<>();
7          list.add("W");
8          list.add("X");
9          list.add("Y");
10         list.add("Z");
11         for (String s : list) {
12             if (s.equals("X")) {
13                 list.remove(s);
14             }
15         }
16         System.out.println(list);
17     }
18 }
```

13行目を実行するとどうなるか。

A. `[W, Y, Z]`と出力される(例外は起きない)

B. 11行目のループ中に`ConcurrentModificationException`がスローされる

C. 16行目で`ConcurrentModificationException`がスローされる

D. 13行目でコンパイルエラーになる

E. `[W, Y, Z]` → 16行目で`ConcurrentModificationException`がスローされる

<a id="復習5-1b"></a>
### 復習5-1b

```java
1  import java.util.Set;
2  import java.util.TreeSet;
3
4  class Ticket {
5      int number;
6      Ticket(int number) { this.number = number; }
7      public String toString() { return "Ticket" + number; }
8  }
9
10 public class Main {
11     public static void main(String[] args) {
12         Set<Ticket> set = new TreeSet<>();
13         set.add(new Ticket(10));
14         System.out.println(set);
15     }
16 }
```

13行目を実行するとどうなるか。

A. `[Ticket10]`と出力される

B. 13行目で`ClassCastException`がスローされる

C. 12行目でコンパイルエラーになる(`Ticket`が`Comparable`を実装していないため)

D. 14行目で`ClassCastException`がスローされる(2個目以降の要素がないので1個目は例外にならない)

E. 13行目で`NullPointerException`がスローされる

<a id="復習5-2b"></a>
### 復習5-2b

```java
1  import java.util.ArrayList;
2  import java.util.Collections;
3  import java.util.List;
4
5  public class Main {
6      public static void main(String[] args) {
7          List<String> list = new ArrayList<>();
8          Collections.addAll(list, "apple", "Banana", "cherry");
9          Collections.sort(list, String.CASE_INSENSITIVE_ORDER);
10         System.out.println("case-insensitive: " + list);
11         Collections.sort(list);
12         System.out.println("after sort(): " + list);
13     }
14 }
```

10行目・12行目の出力はどうなるか。

A. `[apple, Banana, cherry]` → `[apple, Banana, cherry]`(変化なし)

B. `[apple, Banana, cherry]` → `[Banana, apple, cherry]`

C. `[Banana, apple, cherry]` → `[apple, Banana, cherry]`

D. 9行目でコンパイルエラーになる(`String.CASE_INSENSITIVE_ORDER`は存在しない)

E. 11行目で`IllegalStateException`がスローされる

<a id="復習6-1b"></a>
### 復習6-1b

```java
1  import java.util.Collections;
2  import java.util.HashSet;
3  import java.util.Set;
4
5  public class Main {
6      public static void main(String[] args) {
7          Set<Integer> base = new HashSet<>();
8          base.add(1);
9          base.add(2);
10         Set<Integer> view = Collections.unmodifiableSet(base);
11         base.add(3);
12         System.out.println(view.size());
13         view.add(4);
14     }
15 }
```

12行目・13行目を実行するとどうなるか。

A. `2` → 13行目で`UnsupportedOperationException`がスローされる

B. `3` → 13行目で`UnsupportedOperationException`がスローされる

C. `3` → `4`が追加され`view`のサイズが`4`になる

D. `2` → 13行目は何も起きず正常終了する

E. 10行目でコンパイルエラーになる

<a id="復習6-2b"></a>
### 復習6-2b

```java
1  import java.util.HashMap;
2  import java.util.Map;
3
4  public class Main {
5      public static void main(String[] args) {
6          Map<String, Integer> base = new HashMap<>();
7          base.put("A", 1);
8          Map<String, Integer> copy = Map.copyOf(base);
9          base.put("B", 2);
10         System.out.println("copy: " + copy);
11         copy.put("C", 3);
12     }
13 }
```

10行目・11行目を実行するとどうなるか。

A. `copy: {A=1}` → 11行目で`UnsupportedOperationException`がスローされる

B. `copy: {A=1, B=2}` → 11行目で`UnsupportedOperationException`がスローされる

C. `copy: {A=1}` → `C=3`が追加され`{A=1, C=3}`になる

D. 8行目でコンパイルエラーになる(`Map`に`copyOf()`は存在しない)

E. 8行目で`NullPointerException`がスローされる
