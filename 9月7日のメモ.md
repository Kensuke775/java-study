# 9月7日のメモ ― ラッパークラスの不変性とキャッシュ

## 1. ラッパークラスはイミュータブル(不変)

`Integer`, `Long`, `Double`, `Boolean`, `Character`など、プリミティブ型に対応するラッパークラスは、**中身を直接書き換えるメソッドを一切持っていません**。「値の変更ができない」とは、「そのインスタンスの中の値そのものを書き換える手段が存在しない」という意味です。

```java
Integer a = Integer.valueOf(5);
a = a + 1; // 見た目は「aの中身を6に変えた」ように見える
```

実際に起きているのは:

1. `a`は最初、中身`5`を持つ`Integer`インスタンスを指している。
2. `a + 1`は、その`5`を取り出して`6`を計算し、**新しい**`Integer(6)`**インスタンスを作る**。
3. `a`という変数を、その**新しいインスタンス**に**差し替える(再代入する)**。

`5`という値を持っていた元のインスタンス自体は一切変更されない。ずっと`5`のまま、誰にも参照されなくなるだけ。

### ミュータブルなクラスとの対比


|         | 例                               | 挙動                                             |
| ------- | ------------------------------- | ---------------------------------------------- |
| ミュータブル  | `StringBuilder`                 | `sb.append("B")`は同じインスタンスの中身を直接書き換える(実体は1個のまま) |
| イミュータブル | `Integer`, `String`, その他ラッパークラス | 操作すると常に**別の新しいインスタンス**が作られ、変数はそちらに差し替わる        |




## 2. 「変数」と「オブジェクト」を分けて考える

「`a`が`6`になった」という感覚自体は間違っていない。ただし変わったのは**「`a`という変数がどのオブジェクトを指しているか」**であって、**「`5`という中身を持つオブジェクト自体」**は変わっていない。この2つを区別するのがポイント。

**証拠(2つの変数で同じオブジェクトを共有させて検証):**

```java
Integer a = Integer.valueOf(500);
Integer b = a;      // aと全く同じオブジェクトを指す
a = a + 1;           // aだけ新しいオブジェクトに差し替わる
// a = 501, b = 500 のまま。bは一切影響を受けない
```

もし`Integer`がミュータブルだったら、`a`を書き換えた時点で同じ実体を指す`b`も一緒に変わってしまうはず。しかし実際は`b`は無傷 → 「新しいインスタンスに差し替えているだけ」の動かぬ証拠。

### JavaScriptとの対応(感覚はそのまま使える)


|             | JavaScript                    | Java                                    |
| ----------- | ----------------------------- | --------------------------------------- |
| 書き換わらない(不変) | `number`, `string`, `boolean` | プリミティブ、ラッパークラス、`String`                 |
| 書き換わる(可変)   | 配列、オブジェクト                     | 配列、`ArrayList`、`StringBuilder`、普通の自作クラス |


普段のコーディング感覚はJSと同じでよい。ただし`==`の意味だけはJavaの方が厳密(次章参照)。

## 3. ラッパークラスのキャッシュ(`==`が`true`になる範囲)

整数系ラッパークラスは、一定範囲の値をあらかじめJVMがキャッシュしており、`valueOf()`経由(オートボクシング含む)でその範囲の値を作ると**同じインスタンスが再利用される**。

**検証結果の表:**


| ラッパークラス     | キャッシュ範囲             | 備考                  |
| ----------- | ------------------- | ------------------- |
| `Byte`      | 全範囲(`-128`〜`127`)   | `byte`自体がこの範囲しか無い   |
| `Short`     | `-128`〜`127`        |                     |
| `Integer`   | `-128`〜`127`        | 最も出題されやすい           |
| `Long`      | `-128`〜`127`        |                     |
| `Character` | `0`〜`127`           | 文字コードに負数は無いため下限が`0` |
| `Boolean`   | `true`/`false`の2値のみ | 実質常にキャッシュ           |
| `Float`     | **キャッシュ無し**         | 常に新しいインスタンス         |
| `Double`    | **キャッシュ無し**         | 常に新しいインスタンス         |


**注意点:** キャッシュが効くのは`valueOf()`経由(オートボクシング含む)のときだけ。`new Integer(100)`のように**コンストラクタで明示的に**`new`**した場合は、値が範囲内でもキャッシュを無視して必ず新しいインスタンス**が作られる(`new Integer(int)`は非推奨/削除予定のメソッド)。

## 4. 実際に解いた問題(復習用)



### 問題1: イミュータブル性の確認

```java
public class Main {
    public static void main(String[] args) {
        Integer x = Integer.valueOf(500);
        Integer y = x;
        x = x + 1;
        System.out.println(x + " " + y + " " + (x == y));
    }
}
```

**答え:** `501 500 false` — `x`は新しいインスタンスに差し替わり、`y`は元の`500`のインスタンスのまま。別インスタンスなので`==`は`false`。

### 問題2: `new`とキャッシュ範囲外の複合パターン

```java
public class Main {
    public static void main(String[] args) {
        Integer a = 100;
        Integer b = new Integer(100);
        Integer c = 200;
        Integer d = 200;
        System.out.println((a == b) + " " + (c == d));
    }
}
```

**答え:** `false false`

- `a == b`: `b`は`new`で明示生成しているため、値がキャッシュ範囲内(`100`)でもキャッシュを無視 → 別インスタンス → `false`
- `c == d`: どちらもオートボクシングだが、`200`はキャッシュ範囲外 → 別インスタンス → `false`

(もし`b`が`Integer b = 100;`だったら`a == b`は`true`になっていた。)

## 5. その他の小ネタ

- `getClass()`**で実際の型を確認できる**(`obj.getClass().getSimpleName()`)。ただしプリミティブ型には使えない(オブジェクトではないため)。
- **暗黙の拡大変換(widening)**: `Double.valueOf(20)`のように`int`を渡しても、`int → double`は自動変換される(情報が失われない方向の変換は暗黙でOK)。ただしこれは引数がプリミティブの`double`の場合の話で、引数がラッパークラス`Double`(参照型)なら`int`からの自動変換は無い。
- `equals()`**はオブジェクト(参照型)にしか呼べない**。`int i = 1000; i.equals(...)`はコンパイルエラー(`intは間接参照できません`)。`Integer`(参照型)であれば普通に呼べる。

## 6. `parseXxx`は「厳密な数値文字列」しか受け付けない

```java
int iValue = Integer.parseInt("12.5");   // NumberFormatException
long lValue = Long.parseLong("42.5");    // NumberFormatException
```

`parseInt`/`parseLong`は整数専用のパーサー。`.`のような数字以外の文字が1つでも混ざっていると、その場で解析失敗と判定され`NumberFormatException`(実行時例外)がスローされる。「賢く丸める/切り捨てる」のではなく、そもそも`int`/`long`には小数を表す手段がないため仕様として例外にしている。小数を含む文字列を数値化したいなら`Double.parseDouble(...)`を使い、そこから`int`にしたければ明示的に`(int)`キャストか`.intValue()`(切り捨て)を挟む必要がある。

## 7. Integerの不変性・応用問題

```java
Integer x = 1;
Integer y = x;
x += 1;
System.out.println(x + " " + y + " " + (x == y));
// 2 1 false
```

`x += 1`は新しい`Integer(2)`を作って`x`だけを差し替える。`y`は`x += 1`より前に「その時点で`x`が指していたInteger(1)」への参照をコピーしただけなので、後から`x`が別インスタンスに差し替わっても影響を受けない。別インスタンスなので`x == y`は`false`。

## 8. ジェネリクス復習(chap1/3〜10、problem2.mdより)

`problem2.md`(8問、全問javac/java検証済み)の結果は**6/8正解**、問題8・9が不正解。

| 問題 | 回答 | 正解 |
| --- | --- | --- |
| 3 | A | A ⭕ |
| 4 | C | C ⭕ |
| 5 | A | A ⭕ |
| 6 | B | B ⭕ |
| 7 | C | C ⭕ |
| 8 | A | **C** ❌ |
| 9 | C | **D** ❌ |
| 10 | B | B ⭕ |

### 問題4の処理の流れ

```java
3  Box<Integer> box1 = new Box<>();
4  box1.set(10);
5  Box box2 = box1;        // ← raw型で受ける
6  box2.set("Gold");       // ← unchecked警告だけでコンパイルは通る
7  int x = box1.get();     // ← ここでCCE

```

1. `box1`は`Box<Integer>`。内部の`obj`フィールドに`Integer(10)`が入っている。
2. `Box box2 = box1;`で**同じインスタンス**をraw型の変数`box2`からも指すようにする。raw型なのでコンパイラの型チェックは効かない。
3. `box2.set("Gold")`は「`Box`(raw)の`set(Object)`」として扱われるので、型引数`T`のチェックなしで`String`を`obj`フィールドに書き込める。この時点で`box1`が指すのと同じオブジェクトの中身は`"Gold"`に置き換わっている。
4. `int x = box1.get();`は静的には`Box<Integer>.get()`なので、コンパイラは戻り値を暗黙に`(Integer) box1.get()`という形にキャストするコードを埋め込む。
5. 実行時、`get()`が実際に返すのは`"Gold"`(String)。これを`Integer`にキャストしようとして**7行目で**`ClassCastException`。

つまり「危険な代入」をしたのは6行目だが、実際に爆発するのは「型どおり安全に使おうとした」7行目、というのが罠。

### 問題5の解説

```java
12  list2.add(new Box<>());
13  list2.get(0).set("Hello");
14  println(list1.get(0) + list2.get(0).get());

```

- `list2`は`List<Box<String>>`と宣言済み。`add(...)`の引数の型は`Box<String>`だとコンパイラが分かっている(ターゲット型)ので、`new Box<>()`の`<>`はその文脈から`Box<String>`だと**推論できる**。
- `list2.get(0)`の戻り値は`Box<String>`だと静的に分かっている。だから`.get()`の戻り値も`String`だと分かっており、**キャスト不要**。`(String) list2.get(0).get()`のような明示キャストを書かなくても、コンパイラが暗黙に正しい型として扱ってくれる。

> `list2.add(new Box<>());<>の中は省略でくるの？` はい。ダイヤモンド演算子はメソッド引数・変数宣言・戻り値など「型が確定している文脈(ターゲット型)」であれば省略できます。問題5の選択肢Bの「ネストしたジェネリクスは`<>`が使えない」は誤りです。

> `list2.get(0).get()のget()は中身の指定はしなくていいの？` はい不要です。`list2`の宣言(`List<Box<String>>`)の時点で`Box`の中身が`String`だと決まっているので、`get()`の戻り値の型はコンパイラが自動的に`String`だと解決してくれます。

### 問題6:「修飾子」は他に何がある?

明示的型引数を書く形は`修飾子.<型引数>メソッド名(...)`。修飾子として使えるのは主に:

- クラス名(static呼び出し): `Main.<Integer>foo()`
- インスタンス参照(インスタンスメソッド呼び出し): `obj.<Integer>foo()`
- `this`(同一クラス内から): `this.<Integer>foo()`
- `super`(親クラスのメソッドを明示): `super.<Integer>foo()`

修飾子なしでいきなり`<Integer>foo()`と書くことはできません(文の先頭が`<`になり構文エラー)。

### 問題8:「3, "7"は文字列比較?」

いいえ、文字列比較ではありません。これは**実行時の話ですらなく、コンパイル時の型推論の失敗**です。

```java
static <T extends Comparable<T>> T max(T a, T b) { ... }
max(3, "7");

```

呼び出し側は`int`(→オートボクシングで`Integer`)と`String`という2つの異なる型を渡している。`max`は`T a, T b`という**同一の型**`T`**を要求するメソッド。コンパイラは「**`a`**は**`Integer`**、**`b`**は**`String`**、でも**`T`**は1つしかない」という矛盾を検出し、**`T`**をどちらの型にも決められないため8行目でコンパイルエラー**になります(`.compareTo()`が呼ばれることすらない)。だから「文字列として`"3".compareTo("7")`のように比較される」という実行時挙動には一切到達しません。

### 問題9:「コンパイルは通る?参照型のIntegerとObjectは引数として渡せる?」

はい、コンパイルは通ります。

```java
static void addObject(Object[] arr) { arr[0] = "changed"; }
Integer[] iArr = {1, 2, 3};
addObject(iArr);   // ← 7行目、コンパイルOK

```

- 配列は**共変**という特別ルールがあり、`Integer[]`は`Object[]`の一種として扱えるため、`Integer[]`を`Object[]`引数に渡すのは合法(コンパイルエラーにならない)。
- ただし実体(ヒープ上に作られた実際の配列)は最後まで`Integer[]`のまま。JVMは配列に値を**格納する瞬間**に「この配列の本当の要素型と、入れようとした値の型が合っているか」を実行時にチェックしている。
- `arr[0] = "changed";`(3行目)で`String`を`Integer[]`の実体に入れようとした瞬間、このチェックに引っかかり**`ArrayStoreException`**がスローされる。

なので正解は「コンパイルは通る。**実行時に**3行目で`ArrayStoreException`」＝D。Cの「7行目でコンパイルエラー」という回答は、配列の共変ルールを見落としていたための誤りです。ジェネリクスの`List<Integer>`はこの共変が起きない(そもそも`List<Integer>`を`List<Object>`に代入すること自体がコンパイルエラーになる)ので、「配列とジェネリクスコレクションでは挙動が違う」という対比がポイントです。

## 9. 今日作成・更新したファイル

- `JavaGold/_study/1章/1-基本データ型と参照型の型変換/problem_dataType.md` — 問題4-1(`parseInt`のNumberFormatException)、問題5-1(`Integer`不変性の応用)を追加
- `JavaGold/_study/1章/1-基本データ型と参照型の型変換/problem_dataType2.md` — 問題4-1(`Long.parseLong`版)を追加
- `JavaGold/_study/1章/2-ジェネリクス/problem2.md` — chap1/3〜10を題材にした新規8問(全問javac/java検証済み)。結果は6/8正解、問題8・9が不正解
- `JavaGold/_study/1章/2-ジェネリクス/mistakes.md` — 上記で間違えた問題8・9を再挑戦用にピックアップして新規作成

